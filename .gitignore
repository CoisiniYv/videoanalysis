__pycache__/
*.pyc
.DS_Store
.env
.env.*
*.egg-info/
dist/
build/
.venv/

# Debug and manual inspection artifacts (NOT committed)
debug/
manual-inspection/
temp/

# Runtime media output (NOT committed)
media/

# Runtime-generated config artifacts written by scripts/config/* (NOT committed)
infra/generated/
modules/savant_security/config/cameras.generated.yml

# Test output files (NOT committed)
testVideo/test_output_pose.mp4

# Model files (NOT committed) — see docs/model_assets_manifest.md.
# Engines are machine/GPU-specific; weights / ONNX must be sourced
# manually per the manifest, never committed.
*.pt
*.pth
*.ckpt
*.onnx
*.engine
yolomodel/

# Binaries (NOT committed)
redis-cli

# Python
.pytest_cache/
*.log
